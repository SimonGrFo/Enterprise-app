spring:
  security:
    user:
      name: admin
      password: admin
  datasource:
    url: jdbc:postgresql://db:5432/Chilling_ton
    username: postgres
    password: wowiewowser
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

jwt:
  secret: 16O7TpZnQ8Nz1tzNW0q66lrHP2t1znNNWioFxohtOZSqeOvVSn1NdIw2fSv1K1OKSapLWhR6ntjBNcl630mXHQ
  expiration: 3600000 # in milliseconds (e.g., 1 hour)